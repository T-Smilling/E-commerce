spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://192.168.1.4:3306/ecommerce?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_ROOT_PASSWORD:pbhyclls}
    hikari:
      connection-timeout: 5000
    driver-class-name: com.mysql.cj.jdbc.Driver

    properties:
      mail:
        smtp:
          auth: true
        starttls:
          enable: true

  mail:
    from: nkswin@gmail.com
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_USER}
    password: ${EMAIL_PASSWORD}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
  api-docs:
    enabled: true

logging:
  level:
    root: INFO
    com.javaweb.security: DEBUG